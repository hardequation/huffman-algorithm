We are going to encode in such format:
1. sequence of symbols according to their codes
2. additional zeros in the end for byte format
3. 1 byte contains count of additional zeros
4. symbols_cnt * 3 bytes contains dictionary with symbols, code length and code 
5. 1 byte contains count of distinct symbols in dictionary