Формат zmh файла:
1. Словарь в формате: количество символов в словаре(1 байт) + символ1(1 байт) + длина кода символа1(1 байт) + код символа1 + ...
2. 1 байт с числом дополнительных нулей
3. Дополнительные нули, чтобы длина закодированной строки была кратна 8 для последующего перевода в байты
4. Закодированный текст

## TO DO: сколько можно сэкономить для различных типов файла
## TO DO: почему такие результаты, как их интерпретировать
## TO DO: сравнить с архиваторами zip, rar, 7z